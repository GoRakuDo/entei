/**
 * shadcn/ui AspectRatio — adapted for Entei Player.
 * Thin re-export of @radix-ui/react-aspect-ratio.
 */
import * as AspectRatioPrimitive from '@radix-ui/react-aspect-ratio';

const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
